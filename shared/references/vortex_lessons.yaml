lessons:
  - lesson_id: VORTEX_WARP_SCHED_001
    source_project: Vortex
    applies_to:
      - gpgpu-canonical-state-engine
      - gpgpu-implementation-validator
    topic: warp_scheduler
    principle: "Warp scheduling policy must be represented as architectural state or explicit implementation policy."
    do:
      - "Bind behavior through SPEC_IR, GPU_STATE_IR, and explicit contract tables."
    do_not:
      - "Let a downstream implementation invent behavior not visible to validation."
    evidence_anchor:
      - "ref/skillref/vortex.md"
