schema: rewrite_decision_report_ir
version: v5
type: object
required:
  - verdict
  - rewrite_plan_hash
  - selected_patch_type
  - rejected_patch_types
  - revalidation_route
  - evidence_hashes
properties:
  verdict: enum
  rewrite_plan_hash: string
  selected_patch_type: string
  rejected_patch_types: list
  revalidation_route: list
  evidence_hashes: list
