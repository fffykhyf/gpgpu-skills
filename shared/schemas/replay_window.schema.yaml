schema: replay_window
version: v5-xiangshan
owner_skill: gpgpu-simppa
type: object
required:
  - window_id
  - trigger
  - pre_failure_cycles
  - post_failure_cycles
  - start_cycle
  - end_cycle
properties:
  window_id: string
  trigger: string
  pre_failure_cycles: integer
  post_failure_cycles: integer
  start_cycle: integer
  end_cycle: integer
  checkpoint_id: string

