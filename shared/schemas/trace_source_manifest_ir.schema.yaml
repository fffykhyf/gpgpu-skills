schema: trace_source_manifest_ir
version: v5
type: object
required:
  - manifest_id
  - sources
  - source_hashes
  - missing_sources
  - evidence_completeness
properties:
  manifest_id: string
  sources: list
  source_hashes: map
  missing_sources: list
  evidence_completeness: map
  toolchain_artifact_paths: list
  rtl_module_paths: list
  contract_paths: list
