schema: wsync_drain_contract
version: v5
type: object
required: [sm_id, warp_id, prior_work_count, pending_memory_ops, pending_long_latency_ops, drain_begin, drain_end, release_event]
properties:
  sm_id: integer
  warp_id: integer
  prior_work_count: integer
  pending_memory_ops: integer
  pending_long_latency_ops: integer
  drain_begin: string
  drain_end: string
  release_event: string
