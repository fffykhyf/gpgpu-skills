schema: l2_slice_routing_contract
version: v5
type: object
required: [source_sm_id, request_tag, line_addr, target_l2_slice_id, route_id, ordering_scope]
properties:
  source_sm_id: integer
  request_tag: string
  line_addr: integer
  target_l2_slice_id: integer
  route_id: string
  ordering_scope: string
