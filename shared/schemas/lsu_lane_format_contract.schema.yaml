schema: lsu_lane_format_contract
version: v5
type: object
required: [sm_id, warp_id, instruction_uuid, lane_mask, per_lane_addr, aligned_addr, access_size, byte_enable, store_data_shifted, memory_space, load_format_rule]
properties:
  sm_id: integer
  warp_id: integer
  instruction_uuid: string
  lane_mask: list
  per_lane_addr: list
  aligned_addr: integer
  access_size: integer
  byte_enable: list
  store_data_shifted: list
  memory_space: enum
  load_format_rule: enum
