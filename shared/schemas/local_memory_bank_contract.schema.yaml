schema: local_memory_bank_contract
version: v5
type: object
required: [sm_id, bank_count, bank_mapping_function, conflict_policy, read_during_write_policy, response_hold_register, backpressure_behavior, barrier_visibility_rule]
properties:
  sm_id: integer
  bank_count: integer
  bank_mapping_function: string
  conflict_policy: string
  read_during_write_policy: string
  response_hold_register: bool
  backpressure_behavior: string
  barrier_visibility_rule: string
