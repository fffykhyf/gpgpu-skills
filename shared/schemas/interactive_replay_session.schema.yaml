schema: interactive_replay_session
version: v5-xiangshan
owner_skill: gpgpu-simppa
type: object
required:
  - session_id
  - replay_package
  - commands
  - required_inputs
properties:
  session_id: string
  replay_package: string
  commands: list
  required_inputs: list
  checkpoint_optional: string
commands:
  - step_warp
  - step_cycle
  - break_on_warp
  - break_on_memory_addr
  - break_on_barrier
  - dump_wave
  - dump_state
  - compare_ref

