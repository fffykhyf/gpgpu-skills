schema: checkpoint_package
version: v5-xiangshan
owner_skill: gpgpu-simppa
type: object
required:
  - checkpoint_id
  - workload
  - kernel
  - phase_id
  - weight
  - warmup_interval
  - restore_state
  - schema_versions
  - replay_commands
properties:
  checkpoint_id: string
  workload: string
  kernel: string
  phase_id: string
  weight: number
  warmup_interval: map
  restore_state: map
  schema_versions: map
  replay_commands: map

