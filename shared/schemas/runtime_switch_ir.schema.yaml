schema: runtime_switch_ir
version: v5-xiangshan
owner_skill: gpgpu-runtime
type: object
required:
  - switch_name
  - options
  - selected_by
  - stable_io_shape
  - metrics
properties:
  switch_name: string
  options: list
  selected_by: string
  stable_io_shape: boolean
  metrics: list
  unsupported_options: list

