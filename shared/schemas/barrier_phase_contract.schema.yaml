schema: barrier_phase_contract
version: v5
type: object
required: [barrier_id, scope, phase, participant_set, arrival_bitmap, wait_mask, release_bitmap, lsu_drain_required, release_cycle]
properties:
  barrier_id: string
  scope: enum
  phase: integer
  participant_set: list
  arrival_bitmap: list
  wait_mask: list
  release_bitmap: list
  lsu_drain_required: bool
  release_cycle: integer
