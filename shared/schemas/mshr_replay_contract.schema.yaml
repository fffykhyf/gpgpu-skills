schema: mshr_replay_contract
version: v5
type: object
required: [allocate_on, hold_context, replay_order_policy, fill_completion_event, release_event, mshr_deadlock_guard]
properties:
  allocate_on: string
  hold_context: list
  replay_order_policy: string
  fill_completion_event: string
  release_event: string
  mshr_deadlock_guard: map
