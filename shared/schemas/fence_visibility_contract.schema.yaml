schema: fence_visibility_contract
version: v5
type: object
required: [fence_scope, affected_memory_spaces, drain_begin_event, drain_end_event, pre_fence_write_set, post_fence_read_visibility, completion_condition]
properties:
  fence_scope: enum
  affected_memory_spaces: list
  drain_begin_event: string
  drain_end_event: string
  pre_fence_write_set: list
  post_fence_read_visibility: map
  completion_condition: string
