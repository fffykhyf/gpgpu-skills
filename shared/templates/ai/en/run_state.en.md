# RUN_STATE.yaml

Maintain one compact state file per run. Include run_id, mode, active_skills, touched_artifacts, current_truth, gates, and open_issues. Update only touched deltas during FAST_ITERATION.
