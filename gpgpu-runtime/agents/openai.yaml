interface:
  display_name: "GPGPU Runtime"
  short_description: "Design and review host/device launch, runtime APIs, queues, events, and kernel ABI."
  default_prompt: "Design or review a GPGPU runtime or launch-contract change."
