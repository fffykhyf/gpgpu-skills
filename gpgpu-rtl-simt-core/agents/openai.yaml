interface:
  display_name: "GPGPU RTL SIMT Core"
  short_description: "Specify SIMT RTL as an execution state machine."
  default_prompt: "Use $gpgpu-rtl-simt-core to specify SIMT RTL as an execution state machine."
