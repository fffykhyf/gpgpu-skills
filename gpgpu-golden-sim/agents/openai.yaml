interface:
  display_name: "GPGPU Golden Sim"
  short_description: "Validate simulator artifacts against GPU_STATE."
  default_prompt: "Use $gpgpu-golden-sim to validate simulator artifacts against GPU_STATE."
