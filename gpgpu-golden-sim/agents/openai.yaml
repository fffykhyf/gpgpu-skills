interface:
  display_name: "GPGPU Golden Sim"
  short_description: "Build semantic oracle traces for GPGPU state."
  default_prompt: "Use $gpgpu-golden-sim to define semantic oracle traces for GPGPU state."
